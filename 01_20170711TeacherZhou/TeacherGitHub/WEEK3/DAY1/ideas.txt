首先准备
   css/reset.min.css
   img/1.jpg
   json/product.json

1、先写HTML+CSS，把静态页面，能够实现基本的展示即可 (50%)

2、开始写JS
  1)先写utils.js
     ->toArray
     ->toJSON

  2)AJAX获取数据
    ->创建XHR对象
    ->打开请求的地址
    ->监听状态
        ->状态码为4并且HTTP网络码为200
        ->把获取的结果转换为JSON对象
        ->把对象赋值给一个外部的变量
    ->发送请求

  3)ES6数据绑定
    ->把后面需要排序的日期、价格、热度，存储在每一个LI的相关自定义属性上

  4)首先实现一个方法sortGoods，实现按照日期的升序排列
    ->获取容器UL
    ->再获取UL中的所有LI
    ->把获取的类数组转换为数组
    ->把数组排序,按照日期升序
        ->获取当前LI以及后一项LI的日期自定义属性值
        ->把日期中的-替换为空
        ->比较大小实现排序
    ->把最新排好序的LI重新的增加到页面中
        ->文档碎片优化性能
    ->验证实现的是否正确(执行：sortGoods方法即可)

  5)获取所有的A标签，给第一个绑定点击事件，实现按照日期的升降序排列切换
    ->给当前A一个自定义属性存储-1
    ->点击的时候在原有基础上乘以-1，实现标识在-1以及1之间的切换
    ->在排序的时候，让a-b这部分乘以这个标识即可

  6)THIS的优化，把sortGoods方法中的this修改为当前点击的A，在sortGoods中把之前使用的A标签替换为this/_this

  7)实现多列切换
    ->代替原有绑定事件的代码，给所有的A都绑定点击事件
    ->把当前A的索引传递给sortGoods
    ->在sortGoods的sort排序中，根据传递的索引不同，判断不同的自定义属性名称，获取不同的排序字段

  8)点击当前A的时候，把其余的两个A的排序标识清空为-1